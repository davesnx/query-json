(library
 (name Core)
 (wrapped false)
 (public_name query-json.core)
 (modules :standard \ Repl Term_style Help)
 (libraries
  menhirLib
  json
  sedlex
  str
  unix)
 (preprocess
  (pps ppx_deriving.show sedlex.ppx))
 (instrumentation
  (backend bisect_ppx)))

(menhir
 (flags --strict --external-tokens Lexer --explain --dump)
 (modules Parser))
