let[@react.component] make ~value ~placeholder ~onChange =
  let onChangeHandler event =
    let target = React.Event.Form.target event in
    onChange target##value
  in

  <div className="border-b border-border bg-muted/30">
    <div className="max-w-[1400px] mx-auto px-6 py-4">
      <div className="flex items-center gap-3 relative">
        <label
          htmlFor="query"
          className="absolute right-[100%] mr-4 text-sm font-medium \
                     text-muted-foreground whitespace-nowrap \
                     max-[1400px]:hidden">
          (React.string "QUERY")
        </label>
        <input
          id="query"
          type_="text"
          value
          onChange=onChangeHandler
          placeholder
          className="flex-1 bg-background border border-border rounded-lg px-4 \
                     py-2 text-sm text-foreground \
                     placeholder:text-muted-foreground focus:outline-none \
                     focus:ring-2 focus:ring-ring transition-shadow"
        />
      </div>
    </div>
  </div>
